# Kopiér denne fil til .env og udfyld værdierne.
# .env må ALDRIG committes til git (den er allerede i .gitignore).

# URL til det Google Sheet, der er "Publiceret til web" som CSV.
# Se instruktioner i scripts/sync_events.py eller README.md.
GOOGLE_SHEET_CSV_URL="https://docs.google.com/spreadsheets/d/e/.../pub?output=csv"
